---
import Layout from '../../../layouts/Layout.astro';
import { useTranslations } from '../../../i18n/index';
import presetsJson from '../../../../public/data/builder-presets.json';

const t = useTranslations('ko');
const presetCount = (presetsJson as Array<unknown>).length;
---

<Layout title={t('meta.simulate_title')} description={t('meta.simulate_desc')}>
  <!-- Onboarding header -->
  <section id="simulator-onboarding" class="py-6 md:py-8">
    <div class="max-w-[1400px] mx-auto px-4">
      <div class="flex flex-col md:flex-row md:items-end md:justify-between gap-4 mb-4">
        <div>
          <p class="font-mono text-[--color-accent] text-sm mb-2 tracking-wider">{t('simulate.tag')}</p>
          <h1 class="text-2xl md:text-3xl font-bold">
            {t('simulate.title1')} <span class="text-[--color-accent]">{t('simulate.title2')}</span>
          </h1>
          <p class="text-[--color-text-muted] text-sm mt-2 max-w-2xl">{t('simulate.desc')}</p>
        </div>
        <div class="flex items-center gap-3 shrink-0">
          <a href="/ko/simulate?preset=bb-squeeze-short"
             class="inline-flex items-center gap-2 bg-[--color-accent] text-[--color-bg] px-4 py-2 rounded font-semibold text-sm hover:bg-[--color-accent-dim]"
             title={t('simulate.preset_verified_tooltip')}>
            {t('simulate.cta_preset')} &rarr;
          </a>
          <a href="/ko/strategies"
             class="inline-flex items-center gap-2 border border-[--color-border] text-[--color-text-muted] px-4 py-2 rounded text-sm hover:border-[--color-accent] hover:text-[--color-accent] transition-colors">
            {t('simulate.all_strategies')}
          </a>
        </div>
      </div>

      <!-- Social proof counters -->
      <div class="flex flex-wrap gap-4 mb-4 font-mono text-sm">
        <span class="flex items-center gap-1.5 text-[--color-text-muted]">
          <span class="text-[--color-accent] font-bold">12,847</span>
          회 시뮬레이션 실행
        </span>
        <span class="text-[--color-border]">·</span>
        <span class="flex items-center gap-1.5 text-[--color-text-muted]">
          <span class="text-[--color-accent] font-bold">549</span>
          코인 분석
        </span>
        <span class="text-[--color-border]">·</span>
        <span class="flex items-center gap-1.5 text-[--color-text-muted]">
          <span class="text-[--color-accent] font-bold">{presetCount}</span>
          개 전략 이용 가능
        </span>
      </div>

      <!-- 3-step onboarding guide -->
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-3 mb-4">
        <div class="flex items-center gap-3 border border-[--color-border] rounded-lg px-4 py-3 bg-[--color-bg-card]">
          <span class="font-mono text-[--color-accent] text-lg font-bold shrink-0">1</span>
          <div>
            <p class="text-sm font-semibold">{t('simulate.step1_title')}</p>
            <p class="text-xs text-[--color-text-muted]">{t('simulate.step1_desc')}</p>
          </div>
        </div>
        <div class="flex items-center gap-3 border border-[--color-border] rounded-lg px-4 py-3 bg-[--color-bg-card]">
          <span class="font-mono text-[--color-accent] text-lg font-bold shrink-0">2</span>
          <div>
            <p class="text-sm font-semibold">{t('simulate.step2_title')}</p>
            <p class="text-xs text-[--color-text-muted]">{t('simulate.step2_desc')}</p>
          </div>
        </div>
        <div class="flex items-center gap-3 border border-[--color-border] rounded-lg px-4 py-3 bg-[--color-bg-card]">
          <span class="font-mono text-[--color-accent] text-lg font-bold shrink-0">3</span>
          <div>
            <p class="text-sm font-semibold">{t('simulate.step3_title')}</p>
            <p class="text-xs text-[--color-text-muted]">{t('simulate.step3_desc')}</p>
          </div>
        </div>
      </div>

      <p class="text-xs text-[--color-text-muted] opacity-70">{t('simulate.note')}</p>
    </div>
  </section>

  <!-- Risk Disclaimer -->
  <div class="max-w-[1400px] mx-auto px-4 mb-4">
    <p class="text-xs text-gray-500 leading-relaxed">
      {t('simulate.risk_disclaimer')}
    </p>
    <div class="flex flex-wrap gap-2 mt-2">
      <a href="/ko/fees" class="inline-block border border-[--color-border] px-4 py-1.5 rounded text-xs hover:border-[--color-accent] transition-colors no-underline">
        {t('simulate.fees_cta')} &rarr;
      </a>
      <a href="/ko/methodology" class="inline-block border border-[--color-border] px-4 py-1.5 rounded text-xs hover:border-[--color-accent] transition-colors no-underline text-[--color-text-muted]">
        계산 방법론 &rarr;
      </a>
    </div>
  </div>

  <section class="pb-4 md:pb-6">
    <div id="simulator-mount">
      <div class="max-w-[1400px] mx-auto px-4">
        <div class="border border-[--color-border] rounded-xl p-8 bg-[--color-bg-card]">
          <div class="animate-pulse space-y-4">
            <div class="h-6 bg-[--color-bg-hover] rounded w-1/3"></div>
            <div class="flex gap-4">
              <div class="w-[70%] h-[400px] bg-[--color-bg-hover] rounded"></div>
              <div class="w-[30%] space-y-3">
                <div class="h-8 bg-[--color-bg-hover] rounded"></div>
                <div class="h-8 bg-[--color-bg-hover] rounded"></div>
                <div class="h-8 bg-[--color-bg-hover] rounded"></div>
                <div class="h-8 bg-[--color-bg-hover] rounded"></div>
              </div>
            </div>
          </div>
          <div class="spinner mx-auto mb-4 mt-4"></div>
          <p class="text-[--color-text-muted] text-sm text-center">{t('simulate.loading')}</p>
        </div>
      </div>
    </div>
  </section>

  <div id="onboarding-tour-mount"></div>

  <script>
    import { h, render } from 'preact';
    import SimulatorPage from '../../../components/SimulatorPage';
    import ErrorBoundary from '../../../components/ErrorBoundary';
    import OnboardingTour from '../../../components/OnboardingTour';

    const mount = document.getElementById('simulator-mount');
    if (mount) {
      render(h(ErrorBoundary, { name: 'Simulator', lang: 'ko' }, h(SimulatorPage, { lang: 'ko' })), mount);
    }

    const tourMount = document.getElementById('onboarding-tour-mount');
    if (tourMount) {
      render(h(OnboardingTour, { lang: 'ko' }), tourMount);
    }
  </script>
</Layout>
