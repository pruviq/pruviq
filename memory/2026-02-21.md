### 2026-02-21 — Day summary (by 프루빅)

Time: 2026-02-21 (Asia/Seoul)

Key actions & results:
- Merged PRs and quick wins: PRs #1–#5 processed; quick-wins copy and UX P0 merged to main (hero copy, nav rename Simulate→Backtest, blog copy changes).
- i18n / copy changes: synced en/ko hero.desc → meta.index_desc; adjusted hero subtitle to emphasize transparency.
- UX P0 (applied & merged):
  - Added Hero trust block (verified strategies count + link to /performance).
  - Promoted primary CTA to 'Try Live Demo' (improved prominence, min-height for touch targets).
  - Coin detail: added "Apply this strategy" CTA (pre-fills /simulate?coin=SYMBOL).
- KO SEO fixes: replaced ambiguous link texts ('자세히' → '자세히 보기', footer → '수수료 자세히 보기') across site.
- Broken links & content fixes: replaced deprecated /graveyard links with /strategies; added KO 404 page to cover /ko/404/.
- CI / Automation: added .github/workflows/lighthouse.yml (Lighthouse EN & KO; uploads HTML+JSON artifacts; attempts PR comment when applicable) — branch chore/lighthouse-ci pushed.
- P1 work performed (completed):
  - Meta description fallback / page-level meta descriptions added for major pages (index, simulate, strategies, coins, market, fees, about, learn, blog, compare, performance).
  - Mobile touch-targets: global CSS enforcing min-height:44px for interactive elements (public/touch-fix.css) and Layout updated to include it.
  - Loading/empty state: added basic skeleton loaders for simulator and coin detail pages.
  - External link security: added rel="noopener noreferrer" to external anchors with target="_blank" across source files.
  - Branch cleanup: deleted local + remote branches for feat/ux-p0, chore/lighthouse-ci, fix/prod-links after merge.

Build & deployment:
- npm run build succeeded (Astro build successful). Build output: ~1,281 pages built (dist contains ~1,284 HTML files).
- Production checks: https://pruviq.com → 200 OK. https://api.pruviq.com/market → 200 OK.

Pending / Open items:
- Backend: /coins/stats reported 503 earlier (backend/origin issue). Requires ops: server logs, Sentry traces, process restart or config fix. I cannot perform backend restarts (read-only).
- Lighthouse CI: workflow file added and pushed; to execute and collect artifacts automatically we need GH Actions to run (PR or push triggers exist). For automatic PR commenting / workflow dispatch, GH_TOKEN or gh-auth in this environment is required.
- Best Practices score (Lighthouse) remains impacted by external Cloudflare beacon script (score ~83) — needs ops/Cloudflare config review or script loading adjustments.

Notes & follow-ups:
- MEMORY.md updated with day summary; this file (memory/2026-02-21.md) has been appended with this entry for durable storage.
- Recommended next steps: run Lighthouse CI (collect artifacts) → analyze 80< score issues → implement perf/accessibility fixes; schedule backend incident triage for /coins/stats 503.

End of entry — appended by 프루빅.
