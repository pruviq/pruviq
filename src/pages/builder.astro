---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Strategy Builder - PRUVIQ" description="Build your own trading strategy with no code. Select indicators, set conditions, and backtest on 535+ coins with 2+ years of real data.">
  <section class="py-12">
    <div class="max-w-5xl mx-auto px-4">
      <a href="/strategies" class="text-[--color-text-muted] text-sm hover:text-[--color-accent] transition-colors mb-8 inline-block">
        &larr; Strategy Library
      </a>

      <div id="builder-mount">
        <div class="border border-[--color-border] rounded-xl p-8 bg-[--color-bg-card] text-center">
          <div class="animate-pulse">
            <div class="h-3 w-32 bg-[--color-border] rounded mx-auto mb-3" />
            <div class="h-8 w-64 bg-[--color-border] rounded mx-auto mb-3" />
            <div class="h-4 w-96 max-w-full bg-[--color-border] rounded mx-auto" />
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<script>
  import { h, render } from 'preact';
  import StrategyBuilder from '../components/StrategyBuilder';

  const mount = document.getElementById('builder-mount');
  if (mount) {
    render(h(StrategyBuilder, { lang: 'en' }), mount);
  }
</script>
