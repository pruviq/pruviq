# Daily durable memories — 2026-02-23 (append)

- Node runtime changed to Node v22.22.0 (LTS) on the workspace; npm ci and npm run build succeeded after the switch (builds: 1284 / 1290 pages reported during the day).

- OpenClaw gateway network binding was tightened to loopback (http/ws://127.0.0.1:18789) to resolve prior security errors; this enabled successful local agent session spawns.

- Agent registry and SKILL onboarding:
  - Created SKILL.md templates for core agents (frontend-dev, qa-e2e, ops-sre, data-backtest, security-ci, content-marketing) and pushed to agents/init.
  - Agents registered/available: frontend-dev, qa-e2e, ops-sre, data-backtest, security-ci, content-marketing (agent branches/PRs created for audits and fixes during the day).

- Data pipeline / static refresh:
  - Updated backend/scripts/refresh_static.py to add a fallback calculation when CoinGecko /global fails (compute total_market_cap and total_volume from coins list; compute btc_dominance from BTC market cap) and increased inter-call sleep to avoid free-tier rate limits.
  - Ran refresh_static.py → produced public/data/coins-stats.json (500 coins) and public/data/market.json; files written and used for static-first API responses.

- PRs, tests, and notable code changes created today:
  - PR #13: ReproBadge frontend PoC (merged)
  - PR #14: mobile touch-target CSS fixes (open)
  - PR #15: demo reproducible package added to public/data/reproducible for PoC (open)
  - PR #16: accessibility fixes for /simulate (a11y) + full axe test added (open)
  - PR #17: mobile touch-target audit test (Playwright) added (open)
  - Playwright tests added: tests/accessibility/accessibility.full.spec.ts (axe full report), tests/mobile/touch-target-audit.spec.ts (touch-target audit)
  - Reports written under ./reports/: axe-full-_simulate.json, axe-_simulate.json, pruv iq-top10-summary-2026-02-22.html, pagespeed-home.json, and reports/touch-targets.json (when audit run).

- Ops / production issues:
  - Observed api.pruviq.com/coins/stats returning 503 (reproduced); static JSON fallback and refresh resolved immediate UX for the site, but ops/SRE must investigate backend worker/queue/job health. Requested server logs and Sentry traces for analysis.

- Process & governance changes (durable rule additions):
  - Added "Process Rules" to MEMORY.md: Before modifying any existing page, MUST run `git log --oneline --follow <file>`, identify sections added/modified/removed, report current state (file paths + last commit hashes + summary), and get explicit approval from owner (JEPO). Note: TradingView Economic Calendar iframe was removed in commit a9c648b and needs restoration unless a documented reason remains.

- Security / audits:
  - security audit run locally: 1 low-severity finding (devalue) — no high/critical vulnerabilities found. Recommended `npm audit fix` or dependency bump for devalue.

- Misc / housekeeping:
  - MEMORY.md updated multiple times today with run logs and cron artifacts. Several cron jobs (health-check, build-deploy-check, gh-issues-autofix) ran repeatedly and reported OK after fixes.


## Additional durable notes appended — 2026-02-23 21:25 KST

- User requested an objective, expert-style evaluation of the PRUVIQ site; assistant performed the evaluation and recorded conclusions and priorities:
  - Assessment summary: static build/deploy, frontend quality, and testing infrastructure are well-established; the primary remaining critical risk is backend API availability (api.pruviq.com returning 502/503). Static fallbacks mitigate UX but do not replace ops remediation.
  - Recommended priority order:
    1) Ops: investigate and resolve API 502/503 (collect server logs, Sentry traces, check process/port/queue health, recent deploys).
    2) Review & merge PR #16 (accessibility fixes for /simulate) so fixes reach production.
    3) Run mobile touch-target audit (PR #17) → generate reports/touch-targets.json → patch remaining <44px elements (follow-up PRs or merge PR #14 if appropriate).
    4) ReproBadge production workflow: implement CI to build/publish reproducible packages (preferred) or merge demo PR #15 for a short-term PoC.
    5) SEO content work: draft meta/JSON-LD updates for priority pages (index, simulate, strategies) via content-marketing.
  - Immediate recommended action: B — run immediate diagnostics (curl/health checks) and then open ops ticket; assistant offered to run diagnostics and analyze logs.
  - PR snapshot (state observed on 2026-02-23): PR #13 merged; PR #14, PR #15, PR #16, PR #17 open (see MEMORY.md and repo PRs for details).
  - Process note: Post-compaction audit repeatedly flagged WORKFLOW_AUTO.md and today's memory file; assistant read those files and restored operating protocols. The "Process Rules" were added to MEMORY.md to require git-log/diff/report/approval before modifying pages.


## Agent optimization run started: 2026-02-23 22:20:00 KST
- Immediate diagnostics run: curl checks for api.pruviq.com and site root. Results appended to report files and MEMORY.md.

## User directive: agent & organization upgrade (appended)
- Timestamp: 2026-02-23 22:22:09 KST
- User requested: "Focus on upgrading the assistant (프루빅) and its agent colleagues — their efficiency, working methods, architecture, and capabilities — to the highest, production-grade level. Research, verify, and design concrete improvements; do not repeatedly ask the user for choices; act proactively where safe."
- Source: user messages (message_id: 318, 324 and others on 2026-02-23).

Assistant planned actions (non-destructive, immediate → medium):
1) Immediate diagnostics (non-destructive): API health checks and evidence collection (curl/health) — executed and results saved to workspace artifacts and MEMORY.md.
2) SKILL governance: create SKILL schema and GH Action to validate SKILL.md; enforce CODEOWNERS and branch protection for agent PRs.
3) Agent evaluation harness: implement automated agent tasks runner that scores agent success rate, human intervention rate, and hallucination rate (scorecard) using existing tests (Playwright, a11y, mobile).
4) CI for reproducible packages: draft GitHub Actions workflow to package demo runs, compute result_hash, upload to object storage (R2/S3), and publish package metadata (package_url) to public/data/reproducible.
5) CI integration: ensure Playwright a11y + mobile audits run in PRs and produce artifacts; later consider converting to gating checks.
6) Observability & runbooks: design Prometheus/Grafana metrics, Sentry traces, Alertmanager rules, and incident runbooks (incident-502.md) for ops.

Constraints & safety:
- No destructive ops (server restarts, DB migrations, rollbacks) will be executed without explicit human approval (JEPO/owner).
- Agent actions that change production state will be gated by human approval labels (e.g., needs-ops-approval).

Next steps (assistant will proceed proactively):
- I will continue executing non-destructive steps: finalize SKILL schema, generate agent evaluation harness scaffolding, draft the reproducible-package GH Action and PRs for review. I will NOT merge or perform ops-affecting actions without explicit approval.


## System upgrade PR created (append)
- Timestamp: 2026-02-23 22:24:20 KST
- Created branch: agents/system-upgrade-20260223
- Created PR: chore(system): agent governance, SKILL validation, repro-workflow draft, runbook
  - Purpose: add SKILL schema + validation, agent governance doc, draft reproducible packaging workflow, and incident runbook for API 5xx.
  - Files added: skills/SKILL_SCHEMA.md, scripts/validate_skill.py, .github/workflows/validate-skill.yml, .github/workflows/repro-package-draft.yml, docs/AGENT_GOVERNANCE.md, docs/runbooks/incident-502.md
- Note: All changes are non-destructive scaffolding to improve governance and ops readiness. No production runtime changes made.

