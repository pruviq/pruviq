PRUVIQ — Quick audit & action plan (user-focused)
Date: 2026-02-21

Summary
-------
사용자 관점에서 즉시 효과가 큰 Quick wins를 먼저 적용했습니다. 변경 사항은 안전한 텍스트(문구) 수준이며 빌드 확인 후 로컬 브랜치로 커밋해 두었습니다.

What I changed (immediate)
--------------------------
- Updated copy and labels to surface PRUVIQ의 차별점(투명성):
  - src/i18n/en.ts
    - 'nav.simulate' → 'Backtest'
    - 'hero.subtitle' → 'The only backtesting tool that publishes its failures.'
    - 'hero.desc' → (새 요약 문구) "We have tested 5 strategies on 535 coins. 4 lost money. We published them all. Because you deserve to know what actually works before risking real money."
    - 'blog.coming_soon' / 'blog.coming_desc' / CTA → replaced with "LATEST INSIGHTS" copy (remove 'coming soon' tone)
  - src/i18n/ko.ts (Korean equivalents)
    - 'nav.simulate' → '백테스트'
    - 'hero.subtitle' → '실패도 공개하는 유일한 백테스팅 도구.'
    - 'hero.desc' → 한국어 요약문
    - 'blog.coming_soon' 등 → '최신 인사이트' 계열로 수정
    - 'demo.sl' → '손절' (레이블 정리)

- Build
  - npm run build completed successfully (Astro build, 1,281 pages built).

- Git
  - Created branch: feat/quick-wins-copy
  - Committed the two changed files locally (no push performed).

Why these changes (user POV)
---------------------------
- 첫 화면에서 "검증(verification)" 메시지를 바로 느끼게 하여 5초 테스트에서 "왜 이 플랫폼이 다른가"를 즉시 전달합니다.
- "COMING SOON" 같은 문구는 이미 게시된 콘텐츠의 신뢰를 떨어뜨리므로 톤을 바꿨습니다.
- "Simulate"보다 "Backtest"가 사용자 검색어(또는 직관적 행동)를 더 잘 반영합니다.

Next recommended actions (prioritized)
--------------------------------------
P0 (this week)
- Add homepage trust block linking to /performance (live P&L snapshot + last updated). (file: src/pages/index.astro)
- Backtest result UI: show reproducibility metadata (data_version, engine_version, result_hash, timestamp) and add a "Download reproducible package" button (backend change + S3).
- Set up external uptime monitoring (Upptime / Pingdom) + connect alerts (Slack/Telegram).
- Integrate Sentry (backend) for 5xx tracking.

P1 (1–4 weeks)
- Implement reproducible package PoC (backend job that bundles data snapshot + params + run script).
- CI: Add golden regression tests and look-ahead bias checks (PRs must pass before merge).
- SEO: submit sitemap to Search Console, ensure pages are indexed.

P2 (1–3 months)
- Add verification badges in UI (automated QA pass / manual review).
- Publish in-depth BB Squeeze case study article and link to reproducible package.

Files changed (local branch)
----------------------------
- src/i18n/en.ts
- src/i18n/ko.ts

Commands I ran
--------------
- npm run build (succeeded)
- git checkout -b feat/quick-wins-copy
- git add src/i18n/en.ts src/i18n/ko.ts
- git commit -m "feat(copy): emphasize transparency (hero) + rename nav.simulate→Backtest + blog copy quick-win"

Suggested next Git workflow (I can do this if you want)
------------------------------------------------------
- Push branch and open PR for review: 
  git push origin feat/quick-wins-copy
  gh pr create --fill --title "feat: quick wins — hero copy + nav + blog copy" --body "Quick trust-focused copy updates (no logic changes)."

Notes & safety
--------------
- All edits were non-destructive text changes. Build completed successfully.
- I did NOT push branches or deploy. Advise review and merge on your cadence.
- Backend changes (repro package generation, Sentry) require ops credentials / deployment.

If you want me to continue
--------------------------
Reply with one of:
- "Proceed: push+PR" — I will push the branch and open a PR.
- "Proceed: homepage trust block" — I will implement the homepage block next (create branch + commit + build).
- "Proceed: reproducible PoC" — I will implement PoC for one strategy (backend job + UI link). Ops may be required.
- "Stop/Discuss" — hold for manual review.

---
Generated by 프루빅 (automated quick wins).